export class TotalStudentCount {

    // tslint:disable-next-line:variable-name
    course_code!: string;
    // tslint:disable-next-line:variable-name
    totalstudent_count!: number;
    // tslint:disable-next-line:variable-name
    approved_count!: number;
    // tslint:disable-next-line:variable-name
    reject_count!: number;
    // tslint:disable-next-line:variable-name
    pending_count!: number;

    // tslint:disable-next-line:variable-name
    approve_reject_status!: number;
}
