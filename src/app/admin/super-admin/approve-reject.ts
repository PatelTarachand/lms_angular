export class ApproveReject {
    id!: number;
    // tslint:disable-next-line:variable-name
    status_id!: number;
    // tslint:disable-next-line:variable-name
    approve_reject_msg!: string;
    // tslint:disable-next-line:variable-name
    approve_reject_status!: number;
    // tslint:disable-next-line:variable-name
    course_code!: string;
}
